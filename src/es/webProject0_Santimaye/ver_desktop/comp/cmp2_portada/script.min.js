function hideThisModal() {
    $(".mdl_PortadaText").removeClass("onDisplay")
}

function ModalWheelHandler(e) {
    if (1 == CurrentSection) {
        e = window.event || e; - 1 == Math.max(-1, Math.min(1, e.wheelDelta || -e.detail)) && ($(modalText).removeClass("onDisplay"), $(".cmp_Portada img").hide(), $("footer").fadeOut(), $(".cmp_Portada video").show(), $(".cmp_Portada video")[0].play(), $("nav").hide(), $.fn.fullpage.setAllowScrolling(!1))
    }
}

function PortadaWheelHandler(e) {
    if (1 == CurrentSection) {
        e = window.event || e;
        1 == Math.max(-1, Math.min(1, e.wheelDelta || -e.detail)) ? ($(modalText).addClass("onDisplay"), $(".cmp_Portada img").show(), $("footer").fadeIn(), $("nav").show(), $(".cmp_Portada video").hide(), $(".cmp_Portada video")[0].pause(), $.fn.fullpage.setAllowScrolling(!1)) : ($.fn.fullpage.moveTo(2), $.fn.fullpage.setAllowScrolling(!0))
    }
}

function updatePlayVideo(e) {
    1 == e && ($(".cmp_Portada video").show(), $(".cmp_Portada video")[0].play())
}
var vid = document.querySelector("video");
vid.onended = function () {
    $.fn.fullpage.moveSectionDown()
};
var modalText = $(".mdl_PortadaText")[0];
modalText.addEventListener ? (modalText.addEventListener("mousewheel", ModalWheelHandler, !1), modalText.addEventListener("DOMMouseScroll", ModalWheelHandler, !1)) : modalText.attachEvent("onmousewheel", ModalWheelHandler);
var portadaTComp = $(".cmp_Portada")[0];
portadaTComp.addEventListener ? (portadaTComp.addEventListener("mousewheel", PortadaWheelHandler, !1), portadaTComp.addEventListener("DOMMouseScroll", PortadaWheelHandler, !1)) : portadaTComp.attachEvent("onmousewheel", PortadaWheelHandler);